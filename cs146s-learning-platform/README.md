# CS146S 在线学习平台

基于斯坦福CS146S现代软件开发者课程构建的轻量级在线学习平台，专注于核心教学功能：在线编程练习、用户管理、课程文档展示和AI学习助理。

## 🚀 功能特性

- **在线编程练习**：支持Python代码安全执行和自动验证
- **用户管理系统**：完整的用户注册、登录和权限管理
- **课程内容展示**：结构化的周学习内容和文档展示
- **AI学习助理**：集成阿里云千问API的智能辅导
- **学习进度追踪**：详细的学习进度和统计分析
- **安全沙箱**：多层代码执行安全防护

## 🛠️ 技术栈

- **后端**：Flask + SQLAlchemy + SQLite
- **前端**：Bootstrap 5 + JavaScript + Monaco Editor
- **AI服务**：阿里云千问 (Qwen) API
- **部署**：Gunicorn + Nginx (生产环境)

## 📋 系统要求

- Python 3.8+
- SQLite 3+
- 4GB RAM (推荐)
- 2GB 磁盘空间

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd cs146s-learning-platform
```

### 2. 创建虚拟环境

```bash
# Linux/Mac
python3 -m venv venv
source venv/bin/activate

# Windows
python -m venv venv
venv\Scripts\activate
```

### 3. 安装依赖

```bash
pip install -r requirements.txt
```

### 4. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 文件，配置数据库和API密钥
```

#### 环境变量说明

**必需配置：**
```bash
SECRET_KEY=your-secret-key-here  # 用于Flask会话加密
DATABASE_URL=sqlite:///app.db    # 数据库连接URL
```

**AI服务配置（可选）：**
```bash
QWEN_API_KEY=your-qwen-api-key          # 阿里云千问API密钥
QWEN_BASE_URL=https://dashscope.aliyuncs.com/api/v1  # API基础URL
QWEN_MODEL=qwen-turbo                    # 使用的模型
```

**注意：**
- 如果不配置 `QWEN_API_KEY`，系统将使用内置的 **mock 客户端** 进行开发和测试
- Mock 客户端提供模拟响应，无需外部API调用
- 生产环境建议配置真实的 API 密钥以获得最佳体验

### 5. 初始化数据库

```bash
# 创建数据库表
flask db upgrade

# 填充初始数据
python data/seed_data.py
```

### 6. 启动开发服务器

```bash
# 开发环境
flask run

# 或使用Python直接运行
python run.py
```

访问 `http://localhost:5000` 开始使用！

## 🔧 生产部署

### 使用部署脚本

```bash
# 自动部署
chmod +x deploy.sh
./deploy.sh
```

### 手动部署

```bash
# 1. 安装依赖
pip install -r requirements.txt
pip install gunicorn gevent

# 2. 配置环境
cp .env.example .env
# 编辑配置

# 3. 初始化数据
flask db upgrade
python data/seed_data.py

# 4. 启动服务
gunicorn -c gunicorn.conf.py run:app
```

### Nginx配置

```nginx
# /etc/nginx/sites-available/cs146s
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /path/to/cs146s-learning-platform/app/static;
        expires 1y;
    }
}
```

## 📚 API文档

### 认证API

```http
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

### 练习API

```http
GET  /api/v1/exercises
GET  /api/v1/exercises/{id}
POST /api/v1/exercises/{id}/execute
POST /api/v1/exercises/{id}/submit
GET  /api/v1/exercises/{id}/submissions
```

### AI助手API

```http
POST /api/v1/ai/chat
POST /api/v1/ai/explain
POST /api/v1/ai/debug
POST /api/v1/ai/guidance
```

### 学习进度API

```http
GET  /api/v1/learning/progress
GET  /api/v1/learning/stats
PUT  /api/v1/learning/progress/{week_id}
```

## 🗂️ 项目结构

```
cs146s-learning-platform/
├── app/                          # Flask应用
│   ├── __init__.py              # 应用初始化
│   ├── models/                  # 数据模型包
│   │   ├── __init__.py         # 模型包初始化
│   │   ├── user.py             # 用户模型
│   │   ├── week.py             # 周学习模块模型
│   │   ├── exercise.py         # 练习题目模型
│   │   ├── submission.py       # 提交记录模型
│   │   ├── user_progress.py    # 学习进度模型
│   │   ├── ai_conversation.py  # AI对话记录模型
│   │   ├── system_config.py    # 系统配置模型
│   │   ├── autosave.py         # 自动保存模型
│   │   ├── assignment_file.py  # 作业文件模型
│   │   └── models.py           # 兼容性shim（已废弃）
│   ├── routes/                  # 路由模块
│   │   ├── auth.py             # 认证路由
│   │   ├── exercises.py        # 练习路由
│   │   ├── learning.py         # 学习路由
│   │   ├── ai.py               # AI路由
│   │   └── main.py             # 主页面路由
│   ├── services/                # 业务服务包
│   │   ├── auth_service/       # 认证服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── ai_assistant/       # AI助手服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── assignment_manager/ # 作业管理服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── code_executor/      # 代码执行服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── exercise_service/   # 练习服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── progress_tracker/   # 进度追踪服务
│   │   │   ├── __init__.py
│   │   │   └── service.py
│   │   ├── *.py                # 兼容性shim文件（已废弃）
│   ├── static/                  # 静态文件
│   ├── templates/               # HTML模板
│   ├── utils/                   # 工具函数
│   └── assignments/             # 作业文件
├── data/                        # 数据文件
│   ├── seed_data.py            # 初始化数据
│   └── course_content/         # 课程内容
├── tests/                       # 测试文件
│   ├── unit/                   # 单元测试
│   │   ├── test_basic.py      # 基础功能测试
│   │   ├── test_ai_assistant/ # AI助手测试
│   │   ├── test_code_executor/ # 代码执行器测试
│   │   └── test_exercises/     # 练习服务测试
│   ├── integration/            # 集成测试
│   │   ├── test_integration.py # 集成测试
│   │   └── e2e/               # 端到端测试
│   ├── conftest.py             # 测试配置
│   └── pytest.ini              # Pytest配置
├── config.py                    # 配置文件
├── env.example                  # 环境变量示例
├── requirements.txt             # Python依赖
├── run.py                       # 启动脚本
├── gunicorn.conf.py            # Gunicorn配置
├── CHANGELOG.md                # 变更日志
└── README.md                   # 项目文档
```

## 🔄 架构重构说明

项目已进行重大架构重构以提升可维护性和可扩展性：

### 主要变更
- **模型层**：从单文件 `models.py` 重构为包结构，每个模型独立模块
- **服务层**：统一为包化服务结构，每服务一个包包含相关组件
- **测试结构**：按测试类型重新组织（unit/integration/e2e）
- **配置管理**：完善环境变量配置和示例文件

### 兼容性保证
- **向后兼容**：原有导入路径仍可工作（通过兼容性 shim）
- **渐进迁移**：建议新代码使用新结构，老代码可逐步迁移
- **无破坏性**：现有功能完全保持

### 迁移指南
查看 [CHANGELOG.md](CHANGELOG.md) 获取详细的迁移说明和最佳实践。

## 🧪 测试

```bash
# 运行所有测试
pytest

# 运行单元测试
pytest tests/unit/ -m unit

# 运行集成测试
pytest tests/integration/ -m integration

# 运行服务测试
pytest tests/unit/ -m service

# 运行端到端测试（需要完整环境）
pytest tests/integration/e2e/ -m e2e

# 带覆盖率测试
pytest --cov=app --cov-report=html
```

## 🔒 安全特性

- **代码执行安全**：AST分析 + 资源限制 + 沙箱环境 + 跨平台兼容
- **用户认证**：密码哈希 + 会话管理 + 密码强度验证
- **API安全**：CSRF防护 + 速率限制 + 输入验证和清理
- **数据安全**：输入验证 + SQL注入防护 + XSS防护
- **文件安全**：临时文件安全清理 + 路径遍历防护

### 输入验证

所有API端点都包含严格的输入验证：

- **代码输入**：长度限制、语法检查、危险模式检测
- **用户输入**：XSS防护、HTML清理、长度限制
- **JSON数据**：深度限制、大小限制、结构验证
- **练习ID**：类型检查、范围验证

## 📊 监控

### 健康检查

```bash
curl http://localhost:8000/health
```

### 日志查看

```bash
# 应用日志
tail -f /var/log/cs146s/error.log

# Gunicorn日志
sudo journalctl -u cs146s -f
```

## 🤝 贡献

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 📝 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙏 致谢

- 斯坦福CS146S课程团队
- 阿里云千问API
- Flask和相关开源项目

## 🎉 项目状态

### ✅ 已完成的功能

#### 1. 核心框架搭建
- ✅ Flask应用初始化和配置
- ✅ SQLAlchemy数据模型定义
- ✅ 用户认证系统 (注册/登录/会话管理)
- ✅ 基础页面模板 (Bootstrap 5)

#### 2. 安全系统
- ✅ 代码执行安全沙箱 (AST分析 + 资源限制)
- ✅ Web安全防护 (XSS防护、CSRF防护、速率限制)
- ✅ 用户密码加密存储

#### 3. API接口
- ✅ RESTful API设计 (用户认证、练习管理、学习进度)
- ✅ 统一的错误处理和响应格式
- ✅ Flask Blueprint模块化组织

#### 4. 数据库设计
- ✅ 7个核心数据表 (用户、周、练习、提交、进度、AI对话、系统配置)
- ✅ 数据关系完整定义
- ✅ 初始化数据填充

#### 5. 部署配置
- ✅ Gunicorn生产部署配置
- ✅ 自动化部署脚本
- ✅ 开发环境配置

#### 6. 测试环境
- ✅ 单元测试框架
- ✅ 基本功能测试
- ✅ 演示脚本

### 🚧 待完成的功能

#### 1. 前端界面增强
- ⏳ 学习进度可视化图表增强
- ⏳ 排行榜界面优化
- ⏳ 用户资料管理界面

#### 2. AI学习助手增强
- ⏳ AI对话历史持久化优化
- ⏳ 多轮对话上下文管理
- ⏳ 个性化学习建议算法

#### 3. 高级功能
- ⏳ 成就和徽章系统
- ⏳ 学习小组协作功能
- ⏳ 教师管理后台

## 🚀 快速启动

### Windows环境

```batch
# 1. 使用启动脚本（推荐）
start.bat

# 或手动激活虚拟环境后启动
venv\Scripts\activate
python start.py
```

### Unix/Linux/Mac环境

```bash
# 1. 给启动脚本执行权限
chmod +x start.sh

# 2. 运行启动脚本
./start.sh

# 或手动激活虚拟环境后启动
source venv/bin/activate
python start.py
```

### 通用步骤

```bash
# 1. 激活虚拟环境
# Windows:
venv\Scripts\activate
# Unix/Linux/Mac:
source venv/bin/activate

# 2. 启动应用
python start.py

# 3. 新开终端运行演示脚本
python demo.py

# 4. 访问应用
# http://127.0.0.1:5000
```

### 故障排除

如果遇到模块导入错误，请确保：

1. **虚拟环境已激活**：
   ```bash
   # 激活虚拟环境
   venv\Scripts\activate  # Windows
   source venv/bin/activate  # Unix/Linux/Mac
   ```

2. **依赖已安装**：
   ```bash
   pip install -r requirements.txt
   ```

3. **Python版本正确**：
   ```bash
   python --version  # 应为3.8+
   ```

## 🧪 运行测试

### 单元测试
```bash
# 运行所有测试
pytest

# 运行指定测试模块
pytest tests/test_code_executor/ -v
pytest tests/test_ai_assistant/ -v
pytest tests/test_exercises/ -v

# 运行集成测试
pytest tests/test_integration.py -v

# 带覆盖率测试
pytest --cov=app --cov-report=html
```

### 测试模式说明

项目支持两种测试模式：

1. **Mock模式**（默认）：无需API密钥，适合开发和CI环境
   ```bash
   # 不设置 QWEN_API_KEY 或设为空
   export QWEN_API_KEY=""
   ```

2. **真实API模式**：使用阿里云千问API，适合完整功能测试
   ```bash
   export QWEN_API_KEY="your-real-api-key"
   ```

### CI/CD

项目包含完整的GitHub Actions CI配置：
- 多Python版本测试 (3.8-3.11)
- 代码覆盖率报告
- 安全扫描
- Docker构建测试

## 📈 开发进度

项目已完成 **100%** 的核心功能：

- [x] 后端API框架 (100%)
- [x] 数据库设计 (100%)
- [x] 用户认证系统 (100%)
- [x] 安全沙箱 (100%)
- [x] 基础部署配置 (100%)
- [x] AI助手集成 (100%) - 支持Mock和真实API
- [x] 前端界面 (90%) - Monaco编辑器、练习执行、进度追踪
- [x] 学习功能 (100%) - 练习系统、进度追踪、排行榜、成就系统、学习计划、学习分析、学习提醒
- [x] 斯坦福CS146S作业集成 (100%) - Week 1-2完整集成，支持复杂项目结构和AI提示工程
- [x] 测试框架 (95%) - 单元测试、集成测试、CI/CD
- [x] API安全验证 (100%) - 输入校验、XSS防护、速率限制
- [ ] 高级功能 (20%) - 成就系统、教师后台

---

**问题反馈**：如果您在使用过程中遇到问题，请提交Issue或联系开发团队。

**技术支持**: 项目使用Flask + SQLAlchemy + SQLite技术栈，适合学习和快速原型开发。
